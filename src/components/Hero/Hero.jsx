const Hero = () => {
    return (
        <div id="hero" className="bg-blue-400 min-h-screen flex justify-center items-center">
            this is banner
        </div>
    );
};

export default Hero;